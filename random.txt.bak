+id (uuid) (PK)
+clientName (varchar)
+company (varchar)
+serviceName (varchar)
+fileUpload (varchar)
+details (text)

+id (uuid) (PK)
+title (varchar)
+description (text)
+isPublished (bool)
+image (varchar)

portofolios
+id (uuid) (PK)
+serviceId (uuid) (FK)
+name (varchar)
+image (varchar)

user
-id (uuid) (PK)
-name (varchar)
-email (varchar)
-role (varchar)
-createdAt (varchar)
-updatedAt (varchar)
-picture (varchar)
-googleId (varchar)
-hashedRefreshToken (varchar)
-phone (varchar)
-department (varchar)
-position (varchar)


-id (uuid) (PK)
-name (varchar)
-email (varchar)
-role (varchar)
-createdAt (varchar)
-updatedAt (varchar)
-picture (varchar)
-googleId (varchar)
-hashedRefreshToken (varchar)
-phone (varchar)
-department (varchar)
-position (varchar)

o_auth_token
+id (uuid) (PK)
+userid (uuid) (FK)
+provider (varchar)
+accessToken (text)
+refreshToken (text)
+scope (varchar)
+tokenExpiresAt (timestamp)
+createdAt (timestamp)
+updatedAt (timestamp)

todos
+id (uuid) (PK)
+userId (uuid) (FK)
+projectId (uuid) (FK)
+pinned (bool)
+title (varchar)
+task (json)
+createdAt (timestamp)
+updatedAt (timestamp)

project_crew
+id (uuid) (PK)
+projectId (uuid) (PK)


project
+id (uuid) (PK)
+createdById (uuid) (FK)
+serviceId (uuid)
+name (varchar)
+clientName (varchar)
+description (text)
+status (varchar)
+startDate (date)
+dueDate (date)
+createdAt (timestamp)
+updatedAt (timestamp)
+driveFolderId (varchar)
+driveWebView (varchar)

notes
+id (uuid) (PK)
+userId (uuid) (FK)
+projectId (uuid) (FK)
+pinned (bool)
+title (varchar)
+content (text)
+createdAt (timestamp)
+updatedAt (timestamp)

folders
+id (uuid) (PK)
+projectId (uuid) (FK)
+createdById (uuid) (FK)
+name (varchar)
+driveFolderId (varchar)
+driveWebViewLink (text)
+createdAt (timestamp)
+updatedAt (timestamp)
+pinned (bool)

document
+id (uuid) (PK)
+projectId (uuid) (FK)
+folderId (uuid) (FK)
+name (varchar)
+fileType (varchar)
+googleDriveField (varchar)
+googleDriveWebViewLink (varchar)
+originalFilename (varchar)
+createdAt (varchar)
+updatedAt (varchar)
+pinned (bool)